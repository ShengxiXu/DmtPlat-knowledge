# 智能工作助手模板系统重构 PRD

## 1. 文档信息

| 项目     | 内容                                        |
| -------- | ------------------------------------------- |
| 产品名称 | DmtPlat AI Knowledge Base Management System |
| 模块名称 | 智能工作助手 — 模板系统                     |
| 文档版本 | v1.0                                        |
| 文档状态 | 待评审                                      |
| 目标读者 | 产品经理、前端开发、后端开发、UI 设计       |

---

## 2. 项目背景与问题

### 2.1 现状

当前智能工作助手提供“模板”功能，用于快速启动各类 AI 创作任务（写作、表格、PPT、报告等）。模板包含输入字段、提示词、输出形式等配置，用户选择模板后填写字段即可生成内容。

### 2.2 核心问题

用户反馈“模板”一词语义不清：

- **概念混淆**：传统意义上的“模板”指固定结构、标题、版式（如 PPT 模板、文档模板），而系统中的“模板”更像是“生成任务配置”。
- **产出不可预期**：创建模板时只能看到字段和提示词，无法直观判断最终输出长什么样。
- **结构能力弱**：PPT、报告等结构化输出缺乏对章节、页面、版式的预先定义，导致生成结果千篇一律，难以满足不同场景的视觉与结构需求。
- **复用粒度粗**：一套 PPT 的视觉/结构无法独立复用到其他创作任务。

---

## 3. 重构目标

1. **概念清晰化**：将“模板”拆分为“创作模板”和“结构模板”两个独立概念。
2. **产出可预期**：在模板创建和选择环节提供“输出样例预览”。
3. **结构可复用**：结构模板可被多个创作模板复用，支持 PPT、报告、邮件等结构化输出。
4. **扩展性增强**：后续可支持用户上传 PPT/Word 母版、团队结构模板市场等高级功能。

---

## 4. 概念定义

### 4.1 创作模板（Creative Template）

> 定义：一套“输入信息 → AI 生成内容”的任务配置。

回答的问题：

- 为谁用？（岗位角色）
- 做什么？（能力类型、任务描述）
- 填什么？（输入字段）
- 怎么生成？（提示词、输出形式）

示例：

- 客户汇报PPT
- 销售周报
- 竞品对比表
- 客服标准回复

### 4.2 结构模板（Structure Template）

> 定义：生成结果的固定结构、章节、页面版式与视觉样式。

回答的问题：

- 输出长什么样？
- 有哪些章节/页面？
- 每个位置放什么内容？
- 用什么视觉风格？

示例：

- 商务正式 8 页 PPT（封面+目录+5 页内容+结尾）
- 科技现代研究报告（摘要+背景+分析+结论+附录）
- 营销邮件三段式（标题+痛点+方案+行动号召）

### 4.3 两者关系

```
创作模板 ──关联──> 结构模板
   │                    │
   ▼                    ▼
决定“生成什么内容”    决定“内容如何组织呈现”
```

- 一个创作模板可以关联 **0 个或 1 个** 结构模板。
- 一个结构模板可以被 **多个** 创作模板复用。
- 对于纯文本、翻译等无结构输出，可不关联结构模板。

---

## 5. 产品架构

### 5.1 模块划分

```
智能工作助手
├── 创作模板层
│   ├── 创作模板市场
│   ├── 我的创作模板
│   ├── 创作模板创建器
│   └── 创作任务执行
├── 结构模板层
│   ├── 结构模板市场
│   ├── 我的结构模板
│   └── 结构模板编辑器
└── 输出样例预览层
    ├── 结构预览
    └── 示例内容填充预览
```

### 5.2 关键流程

```
用户选择创作模板
      │
      ▼
用户选择/确认结构模板
      │
      ▼
用户填写创作字段
      │
      ▼
AI 按创作模板生成内容
      │
      ▼
系统按结构模板套用结构/版式
      │
      ▼
用户获得最终输出（可分别编辑内容与结构）
```

---

## 6. 数据模型

### 6.1 创作模板 CreativeTemplate

```typescript
interface CreativeTemplate {
  id: string; // 模板唯一标识
  roleId: string; // 岗位角色
  abilityId: string; // 能力类型
  name: string; // 模板名称
  description: string; // 模板描述
  icon?: string; // 图标
  outputType: OutputType; // 输出形式
  defaultMode: 'kb' | 'free'; // 默认模式
  recommendedKBs?: string[]; // 推荐知识库
  fields: TemplateField[]; // 输入字段
  promptTemplate: string; // 生成提示词
  structureTemplateId?: string; // 关联结构模板
  example: Record<string, any>; // 示例数据
  visibility: 'personal' | 'team' | 'official'; // 可见性
  isCustom?: boolean; // 是否自定义
}

interface TemplateField {
  id: string;
  type:
    | 'text'
    | 'textarea'
    | 'number'
    | 'select'
    | 'multi_select'
    | 'date'
    | 'file';
  label: string;
  placeholder?: string;
  required?: boolean;
  options?: string[];
  rows?: number;
}
```

### 6.2 结构模板 StructureTemplate

```typescript
interface StructureTemplate {
  id: string;
  name: string;
  description?: string;
  outputType: OutputType; // 适用输出形式
  isSystem: boolean; // 是否系统内置
  thumbnail?: string; // 缩略图
  style: {
    theme?: string; // 视觉主题
    color?: string; // 配色
    ratio?: string; // 页面比例
    font?: string; // 字体风格
    background?: string; // 背景风格
  };
  structure: StructureNode[]; // 结构节点
  placeholders?: Placeholder[]; // 占位符定义
}

interface StructureNode {
  type: 'cover' | 'toc' | 'content' | 'end' | 'section' | 'chapter';
  title: string;
  count?: number; // 重复次数，用于 content
  layout?: string; // 版式标识
  placeholders?: string[]; // 本节点需要的占位符
  children?: StructureNode[]; // 子节点，用于报告等层级结构
}

interface Placeholder {
  id: string;
  label: string;
  type: 'text' | 'list' | 'image' | 'table';
  source?: 'field' | 'generated'; // 来源：用户字段 or AI 生成
  default?: string;
}
```

### 6.3 输出类型枚举

```typescript
type OutputType =
  | 'text'
  | 'markdown'
  | 'table'
  | 'list'
  | 'email'
  | 'ppt'
  | 'qa'
  | 'steps'
  | 'report';
```

---

## 7. 功能需求

### 7.1 创作模板相关

| 功能              | 优先级 | 说明                                          |
| ----------------- | ------ | --------------------------------------------- |
| 创作模板市场      | P0     | 按角色/能力分类展示系统与团队创作模板         |
| 我的创作模板      | P0     | 个人创建、收藏、历史使用的创作模板            |
| 创作模板创建器    | P0     | 四步创建：意图 → 字段 → 提示词 → 关联结构模板 |
| 输出样例预览      | P0     | 创建和选择时预览最终输出形态                  |
| 创作模板编辑/删除 | P1     | 仅个人模板可编辑，官方模板可复制              |
| 团队审核          | P1     | 团队模板提交后需管理员审核                    |

### 7.2 结构模板相关

| 功能           | 优先级 | 说明                                  |
| -------------- | ------ | ------------------------------------- |
| 结构模板市场   | P0     | 按输出形式/视觉风格分类展示           |
| 我的结构模板   | P0     | 个人创建的结构模板                    |
| 结构模板编辑器 | P0     | 定义章节/页面、版式、占位符、视觉风格 |
| 结构模板预览   | P0     | 可视化预览结构骨架和示例内容          |
| 从示例提取结构 | P1     | 上传 PPT/Word/PDF 自动提取结构        |
| 母版文件上传   | P1     | 上传企业 VI 母版生成结构模板          |

### 7.3 生成执行相关

| 功能               | 优先级 | 说明                              |
| ------------------ | ------ | --------------------------------- |
| 内容与结构分离生成 | P0     | AI 先生成内容，系统再套用结构模板 |
| 结构模板切换       | P0     | 生成后可更换结构模板重新排版      |
| 内容编辑           | P0     | 大纲/段落/表格内容可编辑          |
| 结构编辑           | P1     | 增删页面/章节、调整顺序、更换版式 |

---

## 8. 页面交互设计

### 8.1 智能工作助手首页

**布局调整**：

- 顶部：能力/角色筛选 + 搜索
- 中部：**创作模板卡片网格**
- 卡片信息：
  - 模板名称
  - 适用角色标签
  - 输出形式标签
  - 简短描述
  - “输出样例”按钮
- 点击卡片：进入创作模板详情/执行页

### 8.2 创作模板执行页

**三栏布局**：

- **左侧**：字段填写表单
- **中间**：输出样例/结构预览
- **右侧**：结构模板选择 + 生成配置

**交互流程**：

1. 用户选择/确认结构模板
2. 用户填写字段
3. 实时预览根据示例数据渲染结构骨架
4. 点击“生成”后，AI 生成内容并套用结构
5. 生成结果可在“内容视图”与“结构视图”间切换

### 8.3 创作模板创建器（四步）

| 步骤   | 标题         | 内容                                         |
| ------ | ------------ | -------------------------------------------- |
| Step 1 | 定义创作意图 | 选择角色、能力、输出形式、填写名称与描述     |
| Step 2 | 设计输入字段 | 添加字段、设置类型/必填/示例                 |
| Step 3 | 编写生成指令 | 基础模式（自动）/ 高级模式（直接编辑提示词） |
| Step 4 | 关联结构模板 | 选择现有结构模板或跳过；预览输出样例         |

### 8.4 结构模板编辑器

**两栏布局**：

- **左侧**：结构树（可拖拽排序、增删节点）
- **右侧**：节点配置 + 版式选择 + 占位符映射

**节点类型**：

- 封面
- 目录
- 章节页
- 内容页
- 结尾页
- 自定义页

### 8.5 模板市场

**Tab 切换**：

- 创作模板
- 结构模板

**筛选条件**：

- 按角色
- 按能力
- 按输出形式
- 按视觉风格（结构模板）

---

## 9. 用户流程

### 9.1 普通用户：使用模板生成 PPT

1. 进入智能工作助手首页
2. 浏览/搜索创作模板，选择“客户汇报PPT”
3. 进入执行页，确认结构模板为“商务正式8页PPT”
4. 填写字段：汇报主题、汇报对象、核心信息、关键要点
5. 查看左侧实时结构预览
6. 点击“生成”，系统生成 8 页 PPT
7. 切换“大纲/幻灯片”视图查看结果
8. 可更换结构模板重新排版，或编辑内容

### 9.2 高级用户：创建新模板

1. 点击“创建模板”
2. 选择“创作模板”或“结构模板”
3. 创作模板：按四步完成创建，关联已有结构模板
4. 结构模板：定义页面结构、版式、占位符
5. 保存为个人模板或提交团队审核

---

## 10. 价值分析

### 10.1 用户价值

| 价值点       | 说明                                           |
| ------------ | ---------------------------------------------- |
| 降低认知成本 | “创作模板”和“结构模板”语义清晰，用户一看便知   |
| 产出可预期   | 输出样例预览让用户在填写前就知道会得到什么     |
| 内容更专业   | 结构模板保证 PPT/报告具备完整章节和统一视觉    |
| 提升复用率   | 一个结构模板可被多个创作任务复用，减少重复配置 |
| 支持企业定制 | 企业可上传 VI 母版，形成专属结构模板           |

### 10.2 产品价值

- 拉开与通用 AI 写作工具的差距，形成“模板+结构”双重壁垒。
- 为后续团队模板市场、母版上传、智能推荐等高级功能奠定基础。
- 提升用户留存：结构模板沉淀为资产，用户迁移成本增加。

---

## 11. 竞品/对标分析

| 产品       | 创作模板 | 结构模板                | 输出样例预览 | 母版上传 |
| ---------- | -------- | ----------------------- | ------------ | -------- |
| Gamma      | 有       | 强（PPT 结构+视觉主题） | 有           | 无       |
| 讯飞智文   | 有       | 中（主题+版式）         | 有           | 无       |
| WPS AI     | 有       | 中（模板库）            | 有           | 无       |
| 当前系统   | 有       | 弱                      | 无           | 无       |
| 重构后目标 | 强       | 强                      | 强           | 有       |

**差异化方向**：

- 创作模板与结构模板彻底解耦，支持更细粒度复用。
- 支持用户上传 PPT/Word/PDF 母版并自动提取结构。
- 输出样例预览贯穿模板选择、创建、执行全流程。

---

## 12. 实施计划

### 12.1 阶段划分

| 阶段                       | 周期   | 交付物                        |
| -------------------------- | ------ | ----------------------------- |
| 第一阶段：PRD 与设计确认   | 1-2 天 | PRD 定稿、关键页面线框图      |
| 第二阶段：数据层改造       | 2-3 天 | 创作模板/结构模板数据模型落地 |
| 第三阶段：结构模板管理     | 3-4 天 | 结构模板列表、编辑器、预览    |
| 第四阶段：创作流程重构     | 4-5 天 | 模板创建器、执行页三栏布局    |
| 第五阶段：PPT 生成引擎改造 | 4-5 天 | 内容生成与结构套用分离        |
| 第六阶段：模板市场重构     | 2-3 天 | 分 tab 展示两类模板           |
| 第七阶段：联调与验收       | 3-4 天 | 测试用例、Bug 修复、文档更新  |

### 12.2 关键依赖

- 后端：结构模板存储接口、PPT/报告结构套用服务
- 设计：结构模板编辑器 UI、输出样例预览组件
- 多模态模型：从示例文件提取结构的能力

---

## 13. 验收标准

### 13.1 功能验收

- [ ] 创作模板和结构模板在数据层完全分离
- [ ] 模板创建流程为四步，支持输出样例预览
- [ ] 模板市场分为“创作模板”和“结构模板”两个 Tab
- [ ] PPT 生成后可切换“大纲/幻灯片”视图
- [ ] 生成后可更换结构模板并重新排版
- [ ] 结构模板编辑器支持增删改页面节点、设置占位符

### 13.2 体验验收

- [ ] 新用户能在 30 秒内理解“创作模板”与“结构模板”的区别
- [ ] 模板卡片上能直接查看输出样例
- [ ] 创建模板时每一步都有明确说明

---

## 14. 风险与应对

| 风险                   | 影响 | 应对措施                                       |
| ---------------------- | ---- | ---------------------------------------------- |
| 概念拆分后老用户不习惯 | 中   | 保留原有模板入口，增加引导提示和概念说明       |
| 数据迁移复杂           | 中   | 旧模板自动归类为创作模板，默认关联通用结构模板 |
| PPT 结构套用开发量大   | 高   | 先支持系统内置结构模板，再开放自定义编辑       |
| 结构模板编辑器交互复杂 | 中   | 先提供几种常见结构模板，再逐步开放自由编辑     |

---

## 15. 一句话总结

将“模板”拆分为“创作模板”（决定生成什么内容）和“结构模板”（决定内容如何呈现），并在全流程提供输出样例预览，让智能工作助手的模板系统从“任务配置”升级为“可预期、可复用、可定制的企业级内容生成引擎”。
